export const environment = {
  production: true,
  // Update this once the Render service is live (matches render.yaml's service name
  // unless that name was already taken, in which case Render will have suffixed it).
  apiUrl: 'https://sundesh-api.onrender.com/api',
};
